
<div class="center">
  <div class="row">
    <% @products.each do |product| %>
       <div class="col-sm-6 col-md-4">
         <div class="thumbnail">
          <%= image_tag product.image.url(:medium) %>
          <h3><%= product.name %></h3>
          <!--<%= product.description %>-->
          <p><%= number_to_currency(product.price, :unit =>"€") %></p><br>
          <p><%= "Sold by: #{product.user.name}" %></p><br>
          <%= link_to 'Show', product %>
          <% if (user_signed_in?) && (current_user == product.user) %>
            <%= link_to 'Edit', edit_product_path(product) %>
            <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
          <% end %>     

         </div>
       </div>
    <% end %>
  </div>
</div>
<br>
<% if user_signed_in? %>
  <%= link_to 'New Product', new_product_path, class: "btn btn-link" %>
<% end %>      
    
  
  

<!--<h1>Listing Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      
      faIm not lazy with answering but I was busy so sorry for delay
      bo dla mnie strasznie wczesnie zawsze zafone young how young she cos
      <th>Price</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>-->

  <!--<tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.price %></td>
        <td><%= image_tag product.image.url(:medium) %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<hr>
<%= link_to 'New Product', new_product_path, class: "btn btn-link" %>-->
